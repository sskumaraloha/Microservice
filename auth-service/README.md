# Auth Service

Planned for **Step 5** of the course.

Will implement registration, login, JWT + refresh tokens, role-based
authorization, OAuth2 Google login, email verification, OTP verification,
and forgot/reset password, backed by `auth_db`.
See `docs/architecture/` for the lesson once published.
