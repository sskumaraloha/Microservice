# Local development stack for the Enterprise Employee Management System.
# This file grows one block at a time as each course step adds a service or
# a piece of infrastructure.
#
# Step 9  will add: kafka, zookeeper
# Step 10 will add: redis
# Step 11 will add: zipkin
# Step 12 will add: prometheus, grafana
# Step 13 will add: elasticsearch, logstash, kibana
#
# Usage (from the repository root):
#   docker compose -f infra/docker/docker-compose.yml up --build

version: "3.9"

networks:
  ems-network:
    driver: bridge

volumes:
  mysql-auth-data:
  mysql-employee-data:
  employee-documents-data:
  mysql-department-data:

services:
  discovery-server:
    build:
      context: ../..
      dockerfile: discovery-server/Dockerfile
    container_name: ems-discovery-server
    ports:
      - "8761:8761"
    environment:
      EUREKA_SERVER_USERNAME: ${EUREKA_SERVER_USERNAME:-eureka-admin}
      EUREKA_SERVER_PASSWORD: ${EUREKA_SERVER_PASSWORD:-change-me-in-production}
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8761/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  config-server:
    build:
      context: ../..
      dockerfile: config-server/Dockerfile
    container_name: ems-config-server
    ports:
      - "8888:8888"
    environment:
      CONFIG_SERVER_USERNAME: ${CONFIG_SERVER_USERNAME:-config-admin}
      CONFIG_SERVER_PASSWORD: ${CONFIG_SERVER_PASSWORD:-change-me-in-production}
      CONFIG_ENCRYPT_KEY: ${CONFIG_ENCRYPT_KEY:-local-development-symmetric-key-change-me}
      EUREKA_USERNAME: ${EUREKA_SERVER_USERNAME:-eureka-admin}
      EUREKA_PASSWORD: ${EUREKA_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_HOST: discovery-server
    depends_on:
      discovery-server:
        condition: service_healthy
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8888/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  api-gateway:
    build:
      context: ../..
      dockerfile: api-gateway/Dockerfile
    container_name: ems-api-gateway
    ports:
      - "8080:8080"
    environment:
      CONFIG_SERVER_URL: http://config-server:8888
      CONFIG_SERVER_USERNAME: ${CONFIG_SERVER_USERNAME:-config-admin}
      CONFIG_SERVER_PASSWORD: ${CONFIG_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_USERNAME: ${EUREKA_SERVER_USERNAME:-eureka-admin}
      EUREKA_PASSWORD: ${EUREKA_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_HOST: discovery-server
    depends_on:
      config-server:
        condition: service_healthy
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  mysql-auth:
    image: mysql:8.0.36
    container_name: ems-mysql-auth
    environment:
      MYSQL_DATABASE: auth_db
      MYSQL_USER: auth_service
      MYSQL_PASSWORD: ${AUTH_DB_PASSWORD:-change-me-in-production}
      MYSQL_ROOT_PASSWORD: ${AUTH_DB_ROOT_PASSWORD:-change-me-in-production}
    ports:
      - "3307:3306"
    volumes:
      - mysql-auth-data:/var/lib/mysql
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${AUTH_DB_ROOT_PASSWORD:-change-me-in-production}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  auth-service:
    build:
      context: ../..
      dockerfile: auth-service/Dockerfile
    container_name: ems-auth-service
    ports:
      - "8083:8083"
    environment:
      CONFIG_SERVER_URL: http://config-server:8888
      CONFIG_SERVER_USERNAME: ${CONFIG_SERVER_USERNAME:-config-admin}
      CONFIG_SERVER_PASSWORD: ${CONFIG_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_USERNAME: ${EUREKA_SERVER_USERNAME:-eureka-admin}
      EUREKA_PASSWORD: ${EUREKA_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_HOST: discovery-server
      DB_HOST: mysql-auth
      DB_USERNAME: auth_service
      DB_PASSWORD: ${AUTH_DB_PASSWORD:-change-me-in-production}
    depends_on:
      config-server:
        condition: service_healthy
      mysql-auth:
        condition: service_healthy
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8083/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  mysql-employee:
    image: mysql:8.0.36
    container_name: ems-mysql-employee
    environment:
      MYSQL_DATABASE: employee_db
      MYSQL_USER: employee_service
      MYSQL_PASSWORD: ${EMPLOYEE_DB_PASSWORD:-change-me-in-production}
      MYSQL_ROOT_PASSWORD: ${EMPLOYEE_DB_ROOT_PASSWORD:-change-me-in-production}
    ports:
      - "3308:3306"
    volumes:
      - mysql-employee-data:/var/lib/mysql
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${EMPLOYEE_DB_ROOT_PASSWORD:-change-me-in-production}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  employee-service:
    build:
      context: ../..
      dockerfile: employee-service/Dockerfile
    container_name: ems-employee-service
    ports:
      - "8081:8081"
    environment:
      CONFIG_SERVER_URL: http://config-server:8888
      CONFIG_SERVER_USERNAME: ${CONFIG_SERVER_USERNAME:-config-admin}
      CONFIG_SERVER_PASSWORD: ${CONFIG_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_USERNAME: ${EUREKA_SERVER_USERNAME:-eureka-admin}
      EUREKA_PASSWORD: ${EUREKA_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_HOST: discovery-server
      DB_HOST: mysql-employee
      DB_USERNAME: employee_service
      DB_PASSWORD: ${EMPLOYEE_DB_PASSWORD:-change-me-in-production}
      EMPLOYEE_DOCUMENTS_DIR: /var/lib/ems/employee-documents
    volumes:
      - employee-documents-data:/var/lib/ems/employee-documents
    depends_on:
      config-server:
        condition: service_healthy
      mysql-employee:
        condition: service_healthy
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  mysql-department:
    image: mysql:8.0.36
    container_name: ems-mysql-department
    environment:
      MYSQL_DATABASE: department_db
      MYSQL_USER: department_service
      MYSQL_PASSWORD: ${DEPARTMENT_DB_PASSWORD:-change-me-in-production}
      MYSQL_ROOT_PASSWORD: ${DEPARTMENT_DB_ROOT_PASSWORD:-change-me-in-production}
    ports:
      - "3309:3306"
    volumes:
      - mysql-department-data:/var/lib/mysql
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DEPARTMENT_DB_ROOT_PASSWORD:-change-me-in-production}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  department-service:
    build:
      context: ../..
      dockerfile: department-service/Dockerfile
    container_name: ems-department-service
    ports:
      - "8082:8082"
    environment:
      CONFIG_SERVER_URL: http://config-server:8888
      CONFIG_SERVER_USERNAME: ${CONFIG_SERVER_USERNAME:-config-admin}
      CONFIG_SERVER_PASSWORD: ${CONFIG_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_USERNAME: ${EUREKA_SERVER_USERNAME:-eureka-admin}
      EUREKA_PASSWORD: ${EUREKA_SERVER_PASSWORD:-change-me-in-production}
      EUREKA_HOST: discovery-server
      DB_HOST: mysql-department
      DB_USERNAME: department_service
      DB_PASSWORD: ${DEPARTMENT_DB_PASSWORD:-change-me-in-production}
    depends_on:
      config-server:
        condition: service_healthy
      mysql-department:
        condition: service_healthy
    networks:
      - ems-network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8082/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
