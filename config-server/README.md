# Config Server

Planned for **Step 3** of the course.

Will implement Spring Cloud Config Server for centralized, environment-aware
configuration shared across all services, backed by a Git-versioned config
repository. See `docs/architecture/` for the lesson once published.
