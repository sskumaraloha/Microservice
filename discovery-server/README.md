# Discovery Server

Planned for **Step 2** of the course.

Will implement a Netflix Eureka server so every microservice can register
itself and discover others by logical name instead of hardcoded
host:port pairs. See `docs/architecture/` for the lesson once published.
